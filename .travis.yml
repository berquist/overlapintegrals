sudo: false

matrix:
  include:
    # - language: common-lisp
    - language: julia
      julia: 1.1
      before_script:
        - cd julia/OverlapIntegrals
    - language: python
      python: 3.7
      before_install:
        - cd python
        - pip install -r requirements.txt
      script:
        - python -m pytest
    # - language: racket
    # - language: rust
